# Practice_Landing_bakery
My first try in big case. Keep learning HTML/CSS. 
Here I practiced:
- different CSS properties;
- pseudo-classes(hover/active/nth);
- pseudo-elements(before/after);
- CSS position (relative/absolute).  
This design was taken for free in Figma community.